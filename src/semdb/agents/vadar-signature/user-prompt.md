# Corpus `{{corpus_name}}` — propose helper signatures for its image queries

## Queries (SQL)
```sql
{{query_sql}}
```
## Schema fields to extract (from the Schema Designer)
```json
{{schema_json}}
```
## Predefined API
Read `MODULES_SIGNATURES` in `{{semdb_dir}}/vadar/predefined.py`.

Write the proposed `<docstring>/<signature>` blocks to: `{{sig_path}}`
(one file, plain text). Propose the FEWEST helpers necessary.
