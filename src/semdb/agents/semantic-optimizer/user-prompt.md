# Optimize semantic query `{{query_id}}`

Current plan: `{{plan_path}}`
Current best-candidate manifest: `{{candidate_manifest_path}}`
Structured iteration feedback: `{{iteration_feedback_path}}`

Prior candidate manifests:

{{history_manifest_paths}}

Remaining candidate iterations: `{{remaining_iteration_budget}}`
Remaining replan budget: `{{remaining_replan_budget}}`

Write the single structured action to: `{{optimizer_action_path}}`
